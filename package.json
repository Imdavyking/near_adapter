{
  "dependencies": {
    "@near-wallet-selector/core": "^8.9.10",
    "ts-node": "^10.9.2"
  },
  "devDependencies": {
    "@babel/core": "^7.16.12",
    "@babel/preset-env": "^7.16.11",
    "babelify": "^10.0.0",
    "uglify-js": "^3.15.0",
    "eslint": "^8.7.0"
  },
  "scripts": {
    "prebuild": "tsc index.ts",
    "build": "npx browserify index.js -t [ babelify --presets [ @babel/preset-env ] ] | uglifyjs > ./dist/nighty.min.js && mkdir ./dep && mv *.js ./dep && rm -r dep",
    "prepare-publish": "mkdir -p dist && cp ../dist/nighty-min.js ./dist/nighty-min.js",
    "publish-npm-package": "npm run prepare-publish && npm publish --access public",
    "lint": "eslint . --fix",
    "test": "jest",
    "lfs-fix": "git rm --cached --force ../dist/nighty-min.js && git add ../dist/nighty-min.js"
  }
}
